model User {
    id       Int     @id @default(autoincrement())
    email    String  @unique
    name     String?
    password String
    userName String  @unique
    adress   String

    //relações
    payment Payment[]
    orders  Orders[]
}
